# Allowed directories for file operations (comma-separated)
# Security: Only files within these directories can be accessed
ALLOWED_DIRECTORIES=/Users/yourname/Documents,/Users/yourname/Projects

# PII Detection Settings
# Enable automatic masking of secrets and PII in execution results
MASK_SECRETS=true

# Enable aggressive PII masking (emails, IPs, phone numbers, etc.)
# If false, only secrets/credentials are masked
AGGRESSIVE_MASKING=false

# Execution Limits
# Maximum execution time in seconds (default: 30)
EXECUTION_TIMEOUT=30

# Maximum memory usage in MB (default: 256)
MEMORY_LIMIT_MB=256

# Logging
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
